import { useEffect, useState } from "react";
import { AlertTriangle, FileCheck2, FileSpreadsheet, Landmark, Plus, Receipt, Settings2, Users, type LucideIcon } from "lucide-react";
import { toast } from "sonner";
import { useMigrationFinance } from "@/hooks/use-migration-finance.ts";
import { createMigrationTdsChallan, createMigrationTdsDeduction, getMigrationTdsReturnSummary, saveMigrationTdsSettings } from "@/lib/migration-api.ts";
import { formatCompactInr } from "@/lib/real-estate.ts";
import { formatDate } from "@/lib/format.ts";
import { Button } from "@/components/ui/button.tsx";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card.tsx";
import { Badge } from "@/components/ui/badge.tsx";
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog.tsx";
import { Input } from "@/components/ui/input.tsx";
import MigrationDeducteeImport from "./migration-deductee-import.tsx";
import MigrationDeducteeTab from "./migration-deductee-tab.tsx";

const sectionRates: Record<string, number> = { "192": 10, "194A": 10, "194C": 1, "194H": 2, "194I": 10, "194J": 10, "194Q": 0.1, "195": 30 };
const sections = Object.keys(sectionRates);
const quarters = ["2026-27-Q1", "2026-27-Q2", "2026-27-Q3", "2026-27-Q4", "2025-26-Q4"];

export default function MigrationTdsFilingEnhanced() {
  const settings = useMigrationFinance<any[]>("/api/tds/settings");
  const deductions = useMigrationFinance<any[]>("/api/tds/deductions") ?? [];
  const vendors = useMigrationFinance<any[]>("/api/payables/vendors") ?? [];
  const challans = useMigrationFinance<any[]>("/api/tds/challans") ?? [];
  const [tab, setTab] = useState("deductions");
  const [profileOpen, setProfileOpen] = useState(false); const [deductionOpen, setDeductionOpen] = useState(false); const [challanOpen, setChallanOpen] = useState(false);
  const [profile, setProfile] = useState({ tan: "", deductorName: "", deductorType: "Company", address: "", stateName: "", pincode: "", responsiblePersonName: "", responsiblePersonDesignation: "" });
  const [deduction, setDeduction] = useState({ vendorId: "", deducteeName: "", pan: "", section: "194C", date: new Date().toISOString().slice(0, 10), grossAmount: "" });
  const [challan, setChallan] = useState({ quarter: "2026-27-Q2", section: "194C", paymentDate: new Date().toISOString().slice(0, 10), bsrCode: "", challanSerialNumber: "" });
  const [selected, setSelected] = useState<string[]>([]); const [returnType, setReturnType] = useState("26Q"); const [quarter, setQuarter] = useState("2026-27-Q2"); const [summary, setSummary] = useState<any>();
  const rate = sectionRates[deduction.section] ?? 0; const tdsAmount = Math.round((Number(deduction.grossAmount) || 0) * rate) / 100;
  useEffect(() => { if (tab === "returns") getMigrationTdsReturnSummary(quarter, returnType).then(setSummary).catch(() => setSummary(undefined)); }, [tab, quarter, returnType]);
  const tabs: Array<[string, LucideIcon, string]> = [["deductees", Users, "Deductee"], ["deductions", Receipt, "Deductions"], ["challans", FileCheck2, "Challans"], ["returns", FileSpreadsheet, "Quarterly Returns"]];
  const profileData = settings?.[0];
  const unpaid = deductions.filter((item) => !item.challanId && item.quarter === challan.quarter && item.section === challan.section);
  const challanTotal = unpaid.filter((item) => selected.includes(item._id)).reduce((sum, item) => sum + Number(item.tdsAmount || 0), 0);
  const chooseVendor = (id: string) => { const vendor = vendors.find((item) => item._id === id); setDeduction((current) => ({ ...current, vendorId: id, deducteeName: vendor?.name || "", pan: vendor?.pan || "" })); };
  const saveDeduction = async () => { if (!deduction.vendorId || !deduction.grossAmount) { toast.error("Select a vendor and enter gross amount"); return; } try { await createMigrationTdsDeduction({ ...deduction, rate, grossAmount: Number(deduction.grossAmount), pan: deduction.pan || undefined }); toast.success("TDS deduction recorded"); setDeductionOpen(false); window.location.reload(); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not record deduction"); } };
  const saveChallan = async () => { if (!selected.length) { toast.error("Select at least one unpaid deduction"); return; } try { await createMigrationTdsChallan({ ...challan, deductionIds: selected }); toast.success("Challan recorded"); setChallanOpen(false); window.location.reload(); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not record challan"); } };
  const saveProfile = async () => { try { await saveMigrationTdsSettings(profile); toast.success("Deductor profile saved"); setProfileOpen(false); window.location.reload(); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not save profile"); } };
  return <div className="mx-auto w-full max-w-6xl space-y-6 p-4 md:p-8"><div><p className="text-sm text-muted-foreground">TDS Filing</p><h1 className="font-serif text-3xl font-semibold">TDS Filing</h1><p className="text-sm text-muted-foreground">Deductee master, challan tracking, and quarterly e-TDS return data (24Q / 26Q / 27Q)</p></div><div className="flex flex-wrap items-center justify-between gap-3 rounded-lg border bg-card px-4 py-3"><div className="flex items-center gap-3"><Landmark className="size-4" />{profileData ? <span className="text-sm font-medium">{profileData.deductorName} <span className="ml-2 font-mono text-xs text-muted-foreground">{profileData.tan}</span></span> : <span className="text-sm text-amber-600"><AlertTriangle className="mr-1 inline size-3.5" />Deductor (TAN) profile not set - required for filing</span>}</div><Button size="sm" variant="secondary" onClick={() => { if (profileData) setProfile((value) => ({ ...value, ...profileData })); setProfileOpen(true); }}><Settings2 className="size-4" />Deductor Profile</Button></div><div className="flex flex-wrap items-center justify-between gap-2"><div className="flex rounded-md bg-muted p-1">{tabs.map(([value, Icon, label]) => <Button key={value} size="sm" variant={tab === value ? "outline" : "ghost"} onClick={() => setTab(value)}><Icon className="size-3.5" /> {label}</Button>)}</div>{tab === "deductions" && <Button size="sm" onClick={() => setDeductionOpen(true)}><Plus className="size-4" />Record Deduction</Button>}{tab === "challans" && <Button size="sm" onClick={() => setChallanOpen(true)}><Plus className="size-4" />Record Challan</Button>}</div>{tab === "deductees" ? <MigrationDeducteeTab /> : tab === "deductions" && <Card><CardHeader><CardTitle>Deductions and TDS Payable</CardTitle></CardHeader><CardContent>{deductions.length ? <div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b text-left text-xs uppercase text-muted-foreground"><th className="px-3 py-2">Date</th><th className="px-3 py-2">Deductee</th><th className="px-3 py-2">Section</th><th className="px-3 py-2 text-right">Gross</th><th className="px-3 py-2 text-right">TDS</th></tr></thead><tbody className="divide-y">{deductions.map((item) => <tr key={item._id}><td className="px-3 py-2">{formatDate(item.date)}</td><td className="px-3 py-2">{item.deducteeName}</td><td className="px-3 py-2"><Badge variant="secondary">{item.section}</Badge></td><td className="px-3 py-2 text-right">{formatCompactInr(item.grossAmount)}</td><td className="px-3 py-2 text-right font-semibold">{formatCompactInr(item.tdsAmount)}</td></tr>)}</tbody></table></div> : <p className="py-12 text-center text-sm text-muted-foreground">No TDS deductions yet.</p>}</CardContent></Card>}{tab === "challans" && <Card><CardHeader><CardTitle>Challan Tracking</CardTitle></CardHeader><CardContent>{challans.length ? <div className="divide-y">{challans.map((item) => <div className="flex items-center gap-3 px-3 py-3" key={item._id}><FileCheck2 className="size-4" /><div className="flex-1"><p className="font-medium">{item.internalRef} · {item.section} · {item.quarter}</p><p className="text-xs text-muted-foreground">Paid {formatDate(item.paymentDate)}</p></div><strong>{formatCompactInr(item.amount)}</strong></div>)}</div> : <p className="py-12 text-center text-sm text-muted-foreground">No challans recorded yet.</p>}</CardContent></Card>}{tab === "returns" && <Card><CardHeader><CardTitle>Quarterly Returns</CardTitle></CardHeader><CardContent className="space-y-4"><div className="flex flex-wrap gap-3 rounded-lg border p-3"><label className="text-xs text-muted-foreground">Return Type<select className="mt-1 block h-72 h-9 rounded-md border bg-background px-3 text-sm text-foreground" value={returnType} onChange={(e) => setReturnType(e.target.value)}><option value="24Q">24Q - TDS on salary payments</option><option value="26Q">26Q - Other resident payments</option><option value="27Q">27Q - Non-resident payments</option></select></label><label className="text-xs text-muted-foreground">Quarter<select className="mt-1 block h-9 w-48 rounded-md border bg-background px-3 text-sm text-foreground" value={quarter} onChange={(e) => setQuarter(e.target.value)}>{quarters.map((item) => <option key={item}>{item}</option>)}</select></label></div><p className="py-10 text-center text-sm text-muted-foreground">{summary?.rows?.length ? `${summary.rows.length} rows · Gross ${formatCompactInr(summary.totalGross)} · TDS ${formatCompactInr(summary.totalTds)}` : `No ${returnType} data for ${quarter}`}</p></CardContent></Card>}
    <Dialog open={deductionOpen} onOpenChange={setDeductionOpen}><DialogContent><DialogHeader><DialogTitle>Record TDS Deduction</DialogTitle></DialogHeader><div className="grid gap-3 sm:grid-cols-2"><select className="h-9 rounded-md border bg-background px-3 text-sm sm:col-span-2" value={deduction.vendorId} onChange={(e) => chooseVendor(e.target.value)}><option value="">Select vendor *</option>{vendors.filter((item) => item.isActive !== false).map((item) => <option key={item._id} value={item._id}>{item.name}{item.gstin ? ` - ${item.gstin}` : ""}</option>)}</select><Input placeholder="PAN" value={deduction.pan} onChange={(e) => setDeduction({ ...deduction, pan: e.target.value.toUpperCase() })} /><select className="h-9 rounded-md border bg-background px-3 text-sm" value={deduction.section} onChange={(e) => setDeduction({ ...deduction, section: e.target.value })}>{sections.map((item) => <option key={item}>{item}</option>)}</select><Input type="date" value={deduction.date} onChange={(e) => setDeduction({ ...deduction, date: e.target.value })} /><Input type="number" readOnly value={rate} aria-label="TDS rate" /><Input className="sm:col-span-2" type="number" placeholder="Gross amount (INR)" value={deduction.grossAmount} onChange={(e) => setDeduction({ ...deduction, grossAmount: e.target.value })} /><div className="sm:col-span-2 flex justify-between rounded-lg border bg-muted/30 px-3 py-2 text-sm"><span className="text-muted-foreground">TDS to Deduct</span><strong>{formatCompactInr(tdsAmount)}</strong></div></div><DialogFooter><Button variant="secondary" onClick={() => setDeductionOpen(false)}>Cancel</Button><Button onClick={() => void saveDeduction()}>Record Deduction</Button></DialogFooter></DialogContent></Dialog>
    <Dialog open={challanOpen} onOpenChange={setChallanOpen}><DialogContent><DialogHeader><DialogTitle>Record TDS Challan (Form 281)</DialogTitle></DialogHeader><div className="grid gap-3 sm:grid-cols-2"><select className="h-9 rounded-md border bg-background px-3 text-sm" value={challan.quarter} onChange={(e) => setChallan({ ...challan, quarter: e.target.value })}>{quarters.map((item) => <option key={item}>{item}</option>)}</select><select className="h-9 rounded-md border bg-background px-3 text-sm" value={challan.section} onChange={(e) => setChallan({ ...challan, section: e.target.value })}>{sections.map((item) => <option key={item}>{item}</option>)}</select><Input type="date" value={challan.paymentDate} onChange={(e) => setChallan({ ...challan, paymentDate: e.target.value })} /><Input placeholder="Bank BSR code" value={challan.bsrCode} onChange={(e) => setChallan({ ...challan, bsrCode: e.target.value })} /><Input className="sm:col-span-2" placeholder="Challan serial number" value={challan.challanSerialNumber} onChange={(e) => setChallan({ ...challan, challanSerialNumber: e.target.value })} /></div><div className="max-h-56 overflow-y-auto rounded-lg border divide-y">{unpaid.length ? unpaid.map((item) => <label className="flex items-center gap-3 px-3 py-2 text-sm" key={item._id}><input type="checkbox" checked={selected.includes(item._id)} onChange={() => setSelected((current) => current.includes(item._id) ? current.filter((id) => id !== item._id) : [...current, item._id])} /><span className="flex-1">{formatDate(item.date)} · {item.deducteeName}</span><strong>{formatCompactInr(item.tdsAmount)}</strong></label>) : <p className="p-4 text-sm text-muted-foreground">No unpaid deductions for this quarter and section.</p>}</div><div className="flex justify-between rounded-lg border bg-primary/5 px-3 py-2 text-sm"><span>Total Challan Amount</span><strong>{formatCompactInr(challanTotal)}</strong></div><DialogFooter><Button variant="secondary" onClick={() => setChallanOpen(false)}>Cancel</Button><Button onClick={() => void saveChallan()} disabled={!selected.length}>Record Challan Payment</Button></DialogFooter></DialogContent></Dialog>
    <Dialog open={profileOpen} onOpenChange={setProfileOpen}><DialogContent><DialogHeader><DialogTitle>Deductor (TAN) Profile</DialogTitle></DialogHeader><div className="grid gap-3 sm:grid-cols-2"><Input placeholder="TAN" value={profile.tan} onChange={(e) => setProfile({ ...profile, tan: e.target.value.toUpperCase() })} /><Input placeholder="Deductor name" value={profile.deductorName} onChange={(e) => setProfile({ ...profile, deductorName: e.target.value })} /><Input className="sm:col-span-2" placeholder="Address" value={profile.address} onChange={(e) => setProfile({ ...profile, address: e.target.value })} /></div><DialogFooter><Button variant="secondary" onClick={() => setProfileOpen(false)}>Cancel</Button><Button onClick={() => void saveProfile()}>Save Profile</Button></DialogFooter></DialogContent></Dialog>
  </div>;
}


